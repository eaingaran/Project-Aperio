<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd" >

<configuration>
	 <typeAliases>
		<typeAlias alias="user" type="xyz.aingaran.project.aperio.domain.User" />
	</typeAliases>
<!-- 
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" /> can be JDBC/MANAGED

			<dataSource type="POOLED"> can be UNPOOLED/POOLED/JNDI
				<property name="driver" value="${jdbcConnection.DriverClassName}" />
				<property name="url" value="${jdbcConnection.url}" />
				<property name="username" value="${jdbcConnection.username}" />
				<property name="password" value="${jdbcConnection.password}" />
			</dataSource>

		</environment>
	</environments> -->

	<mappers>
		<mapper resource="mybatis/mappers/daoMapper.xml" />
	</mappers>
</configuration>